apiVersion: v1
kind: Service
metadata:
  name: consumer
  labels:
    app: consumer
spec:
  selector:
    app: consumer
  ports:
    - port: 80
      targetPort: {{ .Values.consumer.port }}
      name: http
